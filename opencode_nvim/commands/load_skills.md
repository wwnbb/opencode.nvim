---
description: Load skills.
---

Treat `$ARGUMENTS` as one or more skill names.
Call the `skill` tool exactly once for each requested skill.
If multiple skill names are provided, load each one.
Reply with a brief confirmation listing what was loaded.
